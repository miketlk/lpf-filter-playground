# lpf-filter-playground
A playground project for LPF filter evaluation

Use as follows:
```shell
./build.sh
./lpf-step.app
chmod +x plot.py
./plot.py 
```

It is assumed that `matplotlib` is installed in your Python environment.
